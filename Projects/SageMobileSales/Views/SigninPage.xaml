<prism:VisualStateAwarePage
    x:Class="SageMobileSales.Views.SigninPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SageMobileSales.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="using:Microsoft.Practices.Prism.StoreApps"
    prism:ViewModelLocator.AutoWireViewModel="true"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Uid="SageMobileSales" HorizontalAlignment="Center" TextWrapping="Wrap" FontSize="96" Foreground="Green"/>         
            <Button x:Uid="SignIn" HorizontalAlignment="Center" Command="{Binding SignInCommand}" FontSize="29.333" Background="Green" FontWeight="Bold" BorderBrush="Green" IsEnabled="{Binding isSignInDisabled}"/>
            <ProgressRing Grid.Row="1" Height="70" Width="70" 
                      IsActive="{Binding InProgress, Mode=TwoWay}" 
                      Visibility="{Binding InProgress, Mode=TwoWay,Converter={StaticResource InProgessVisibilityConverter}}"
                      Style="{StaticResource GridViewProgressRingStyle}"/>
        </StackPanel>

    </Grid>
</prism:VisualStateAwarePage>
